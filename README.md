# indproj
Repo for indproj

### Building

Init submodules
```bash
    git submodule update --init --recursive
```

Build the project
```bash
    cd code
    mkdir build
    cd build
    cmake ..
    make
```
